# ゲン＝ガタ限定ルール実装 - 設計仕様書 Phase 1

**作成日**: 2026-07-20  
**最終更新**: 2026-07-21  
**ステータス**: 設計確定（実装前）

---

## 🎯 目的

Battle Spirits の本質的なゲームエンジンをスクラッチから実装する。  
ゲン＝ガタを中心にシンプルなルール定義から始め、将来的に複雑な機能（継召、フラッシュ、スキル）を追加可能な設計。

---

## 📐 層構成

### 層0: マッチ設定（ルール選択）

ゲームの「どんなマッチか」を定義。複数ゲーム、異なるルール形式に対応。

```typescript
type MatchConfig = {
  format: 'standard' | 'limited';
  winCondition: 'best-of-1' | 'best-of-3' | 'best-of-5';
  timeLimit?: number;
  restrictedCards?: string[];
};

type MatchState = {
  config: MatchConfig;
  games: GameState[];
  currentGameIndex: number;
  wins: [number, number];
  matchOver: boolean;
  winner?: 0 | 1;
};
```

**責務:**
- マッチ形式を指定
- 複数ゲーム管理
- n本先取の勝者判定

**拡張性:**
- 構築限定戦のブロック制限
- 新しいマッチ形式の追加

---

### 層1: ゲーム状態の型定義

ゲームに必要な全データの「形」を定義。ルール上意味のある情報と構築上意味を持つ情報を分離。

#### PlayerState: プレイヤー情報

```typescript
type PlayerState = {
  // 基本情報
  life: number;

  // ゾーン
  hand: Card[];
  deck: Card[];
  reserves: number;        // リザーブ（コア数）
  trashCores: number;      // トラッシュ内のコア数
  field: Spirit[];         // 場（スピリット・ネクサス）
  trash: Card[];           // トラッシュ（カード）
  excluded: Card[];        // 除外ゾーン（継召で使われたカード）
};
```

**ゾーン解説:**
- **deck**: デッキゾーン（ゲーム開始時のカード集合、順序を保持）
- **hand**: 手札ゾーン
- **reserves**: リザーブゾーン（プレイヤーが使用可能なコア）
- **field**: フィールドゾーン（スピリット・ネクサスが配置される公開領域）
- **trash**: トラッシュゾーン（破壊されたカード、継召で選ぶ）
- **trashCores**: トラッシュゾーン内のコア領域（リフレッシュフェーズで reserves に戻る）
- **excluded**: 除外エリア（ゲーム領域外。除外されたカードを置く特殊な領域）

---

#### Color: シンボル色の型定義

```typescript
type Color =
  | "red"
  | "purple"
  | "green"
  | "white"
  | "yellow"
  | "blue";
```

---

#### Card: カード定義

```typescript
type Reduction = {
  max: number;          // 軽減可能な最大シンボル数
  colors: Color[];      // 軽減対応のシンボル色
};

type Card = {
  // ─── ルール上意味のある情報 ───
  id: string;
  name: string;
  cardType: 'spirit' | 'nexus' | 'magic';
  cost: number;
  
  // シンボル関連（軽減シンボルとは別）
  symbol?: {
    count: number;      // 通常シンボル数（ライフダメージ計算に使用）
    colors: Color[];    // 各シンボルの色
  };
  exSymbol?: boolean;   // EXシンボルの有無
  
  // 軽減シンボル（コスト軽減用）
  reduction?: Reduction;
  
  lineage: string[];
  text: string;
  levels: CardLevel[];
  attribute?: string;
  effects: Effect[];

  // ─── 構築上意味を持つ情報 ───
  cardNumber?: string;
  rarity?: string;
  blockIcon?: string;
  artworkIcon?: string;
};

type CardLevel = {
  level: 1 | 2 | 3;
  bp: number;
  // ライフダメージは symbol.count から決定（層2ルール）
};

type Effect = {
  trigger: string;      // 誘発条件（具体的な種類は層3.2で定義）
  action: string;
  minLevel?: number;    // Lv依存の発動条件
};
```

**設計のポイント:**
- **Color型**: 型安全性を確保（red, purple, green, white, yellow, blue）
  - タイプミスをコンパイル時に防止
  - IDE補完により開発効率向上
  - ゲームルール上の色を明示的に型定義
- **symbol**: 通常のシンボル（カードが持つ色マーク、軽減シンボルとは別）
  - `symbol.count` はライフダメージ計算に使用（層2ルール）
  - `symbol.colors` は Color[] で型安全性確保
  - 将来 `attackSymbol` などの概念へ拡張可能な設計
- **exSymbol**: EXシンボル（複数存在する場合もある）
- **reduction**: 軽減シンボル（コスト支払い時に軽減可能なシンボル数）
  - `reduction.max`: 軽減可能な最大数（明確で拡張性高い）
  - `reduction.colors`: 軽減対応色を Color[] で型安全に管理
- **ライフダメージの決定方法**（層2ルール）
  - ダメージ値は Card や CardLevel に含まない
  - 層2ルールで「ライフダメージ = symbol.count」と定義
  - BPとダメージは独立した概念
- **levels**: 複数のレベルを配列で管理（Lv1とLv2で BP が異なる）
- **effects**: triggerという情報項目を持てることを定義
  - triggerの具体的な種類（'summon', 'attack'等）は層3.2で定義
  - 層1では「triggerという情報を持てる」という仕組みのみ定義
- **構築情報**: ゲームロジックでは使わない（UI/デッキ構築のみ）

---

#### Spirit: 場のスピリット

```typescript
type Spirit = {
  def: Card;
  level: 1 | 2 | 3;
  cores: number;        // 乗ってるコア
};
```

---

#### ゲーム全体の状態

```typescript
type Phase = 'start' | 'draw' | 'main1' | 'attack' | 'main2' | 'end';

type GameState = {
  players: [PlayerState, PlayerState];
  currentPlayerIndex: 0 | 1;
  currentPhase: Phase;
  gameOver: boolean;
  winner?: 0 | 1;
};
```

---

### 層2: 基本ルール（ゲームの本質）

「何ができるのか」「何をするとどうなるのか」を実装。一度決めたら変わらない基本ルール。

---

#### ZoneStateRule（ゾーン管理ルール）

**定義:**
ゲーム内の各ゾーン（手札、デッキ、場、トラッシュ等）の存在・状態・カード移動を管理する基本ルール。

**責務:**

1. **ゾーンの存在を定義する**
   - 手札ゾーン
   - デッキゾーン
   - 場ゾーン
   - トラッシュゾーン
   - 除外ゾーン

2. **各ゾーンがカードを保持できることを定義する**
   - 複数のカードを保持可能
   - ゾーン間でカードが移動可能

3. **ゾーン間のカード移動ルールを定義する**
   - デッキ → 手札（ドロー時）
   - 手札 → 場（召喚・配置時）
   - 場 → トラッシュ（破壊時）
   - トラッシュ → 手札（継召で選択）

**デッキゾーンの定義:**
- デッキは、ゲーム開始時にプレイヤーが保有するカード集合
- card順序を保持する（配列として管理）
- ゲーム進行中にカードが手札へ移動される領域
- 将来的にシャッフル、デッキ操作等へ拡張可能な構造

**デッキゾーンが担当しないもの:**
- ❌ ドロー処理（いつ・どうやってカードを引くか）→ 層3の責務
- ❌ 初期手札決定 → 層3の責務
- ❌ デッキ切れによる敗北判定 → 層3の責務
- ❌ シャッフル処理 → 将来のルール（CoreRule等）の責務

**手札ゾーン（Hand Zone）の定義:**

手札ゾーンは、デッキから引いたカードをプレイヤーが一時的に保有する領域です。

**責務:**

1. **手札ゾーンの存在を定義する**
   - PlayerState 内に `hand: Card[]` として保持する
   - ゲーム内の正式なゾーンとして定義する

2. **デッキからのカード移動先として機能する**
   - デッキゾーンから手札ゾーンへのカード移動を受け入れる
   - カード移動後の保持・管理

3. **カード集合を保持する**
   - 複数のカードを順序付きで保有可能
   - 層3のゲーム進行から指定されたカードを受け取る

**デッキゾーンとの関係:**

```
デッキゾーン（deck）
    ↓
（ゲーム進行がカード取得指示）
    ↓
手札ゾーン（hand）へ移動
```

- **デッキゾーン**: ゲーム開始時のカード集合を保管する領域
- **手札ゾーン**: デッキから引いたカードを置く領域
- 一方向の移動関係のみ定義

**手札ゾーンが担当しないもの:**
- ❌ ゲーム開始時に何枚引くか → 層3の責務
- ❌ ターン開始時に何枚引くか → 層3の責務
- ❌ カード使用・プレイのタイミング → 層3の責務
- ❌ 手札からカードをプレイする処理 → 層3/効果エンジンの責務
- ❌ 手札枚数による敗北条件 → 層3の責務
- ❌ 対戦相手への非公開処理 → 層4（API/UI）の責務
- ❌ 手札公開ルール → 層4の責務
- ❌ 手札サイズ制限 → 将来のルール（HandSizeRule等）の責務
- ❌ カード効果による手札操作 → 層3.2（効果エンジン）の責務

**トラッシュゾーン（Trash Zone）の定義:**

トラッシュゾーンは、プレイヤーが破壊したカードとコスト支払いによって消費されたコアを保持する領域です。公式ルール通り、カード領域とコア領域の2つの領域に分かれます。

**構造:**

```
PlayerState 内のトラッシュゾーン

trash: Card[]         ← カードトラッシュ
trashCores: number    ← コアトラッシュ
```

**責務:**

1. **トラッシュゾーンの存在を定義する**
   - PlayerState 内に trash と trashCores として保持する
   - ゲーム内の正式なゾーンとして定義する

2. **2つの領域を持つ構造を定義する**
   - カードトラッシュ（trash: Card[]）
   - コアトラッシュ（trashCores: number）

3. **各領域がデータを保持する**
   - カード領域：複数のカードを順序付きで保有
   - コア領域：消費されたコア数をカウント

**カードトラッシュ（trash: Card[]）**

保持するもの：
- 破壊されたスピリット・ネクサス
- デッキから破棄されたカード
- 手札から破棄されたカード
- 使用済みマジックカード（効果解決後）

特性：
- 順序を保持する（配列）
- 個別のカードオブジェクトを保有
- 継召により参照・取得される対象

**コアトラッシュ（trashCores: number）**

保持するもの：
- カード召喚時のコスト支払いで消費されたコア
- 効果によってトラッシュへ置かれたコア

特性：
- 数値でカウント（集約データ）
- リフレッシュフェーズで reserves へ戻される
- 個別のコアを区別しない

**ゾーン間のカード・コア移動ルール:**

```
【カード移動】
field → trash.cards（破壊時）
hand → trash.cards（破棄時）
deck → trash.cards（破棄時）
trash.cards → hand（継召で選択）
trash.cards → excluded（継召実行時）

【コア移動】
reserves → trash.cores（コスト支払い）
trash.cores → reserves（リフレッシュ時）
```

**トラッシュゾーンが担当しないもの:**
- ❌ カードが破壊される時に送る処理 → 層3/効果エンジンの責務
- ❌ コスト支払い処理 → 層3の責務
- ❌ 効果によるカード破棄 → 層3.2の責務
- ❌ 継召などのカード参照処理 → 層3.2の責務
- ❌ リフレッシュ時のコア回収処理 → 層3の責務
- ❌ トラッシュ満杯による敗北判定 → 層3の責務
- ❌ トラッシュの可視化 → 層4（API/UI）の責務
- ❌ トラッシュソート機能 → 将来の拡張機能の責務

**フィールドゾーン（Field Zone）の定義:**

フィールドゾーンは、プレイヤーがスピリットやネクサスを配置して展開する領域です。公開領域として扱われます。

**構造:**

```
PlayerState 内のフィールドゾーン

field: Spirit[]       ← スピリット・ネクサスの配置領域
```

**責務:**

1. **フィールドゾーンの存在を定義する**
   - PlayerState 内に field: Spirit[] として保持する
   - ゲーム内の正式なゾーンとして定義する

2. **スピリット・ネクサス配置領域を定義する**
   - 複数のカードを保有可能
   - 各カードが状態情報を持てる構造

3. **公開領域であることを定義する**
   - フィールド上のカード情報は両プレイヤーが確認可能
   - 特別な効果がない限り表向きで存在する

4. **配置カードの初期状態を定義する**
   - カードがフィールドへ移動した時、指定がない場合：回復状態で置かれる

**カード配置時の初期状態:**

```
配置時のカード状態（特に指示がない場合）

canAttack: true    ← 回復状態（攻撃可能）
exhausted: false   ← 疲労なし
bpBoost: undefined ← 一時的なBP強化なし
coreCount: 0       ← コア0個で開始
```

**配置カードが保持する状態情報:**

- レベル（Lv1 / Lv2）
- コア数（通常コア）
- 魂コア数
- 疲労状態（回復 / 疲労）
- 一時的BP強化（ターン中）
- 一時的BP強化（バトル中）
- 配置コア数
- ステータス効果

**ゾーン間のカード移動ルール:**

```
【カード配置】
hand → field（スピリット召喚、ネクサス配置時）

【カード除去】
field → trash.cards（破壊時）
```

**フィールドゾーンが担当しないもの:**
- ❌ スピリットのアタック処理 → 層3/BattleRuleの責務
- ❌ スピリットのブロック処理 → 層3/BattleRuleの責務
- ❌ BP比較 → BattleRuleの責務
- ❌ バトル結果判定 → BattleRuleの責務
- ❌ カード破壊処理 → 層3/効果エンジンの責務
- ❌ コア配置によるレベル変更 → 層3/LevelingRuleの責務
- ❌ 疲労状態の変更判定 → 層3の責務
- ❌ 表示制御 → 層4（API/UI）の責務

**リザーブゾーン（Reserve Zone）の定義:**

リザーブゾーンは、ゲームに使用するコアが置かれる領域です。プレイヤーが直接使用できるコアを保持します。

**構造:**

```
PlayerState 内のリザーブゾーン

reserves: number    ← プレイヤーが使用可能なコア
```

**責務:**

1. **リザーブゾーンの存在を定義する**
   - PlayerState 内に reserves: number として保持する
   - ゲーム内の正式なコアゾーンとして定義する

2. **プレイヤーが使用可能なコアを保持する**
   - リザーブに存在するコアがプレイヤーの利用可能なコア
   - 数値でカウント管理（実装上の簡略化）

3. **コアの存在場所を定義する**
   - 「リザーブにコアが存在する」という状態を表現する

**コア領域の種類と関係:**

```
【リザーブゾーン】
reserves: number
→ プレイヤーが直接使用可能なコア
→ カード召喚やコスト支払いの対象

【トラッシュゾーン内のコア領域】
trash.cores: number
→ コスト支払いされたコア
→ リフレッシュ時に reserves へ戻される

【ライフゾーン】
life: number
→ ライフとして扱われるコア

【フィールド上のカード】
spirit.coreCount, spirit.placedCores
nexus.coreCount, nexus.placedCores
→ カード上に配置されたコア
```

**コア領域の分離と独立性:**
- リザーブ ≠ トラッシュ内のコア
- リザーブ ≠ 場のカード上のコア
- リザーブ ≠ ライフ
- 各領域は別々に管理される

**ゾーン間のコア移動ルール:**

```
【コア獲得】
reserves に追加

【コスト支払い】
reserves 減少
trash.cores に追加

【リフレッシュ】
trash.cores → reserves へ移動
trash.cores をリセット
```

**リザーブゾーンが担当しないもの:**
- ❌ 毎ターンのコア獲得処理 → 層3の責務
- ❌ コア支払い処理 → 層3の責務
- ❌ 軽減シンボルによる計算 → 軽減シンボルルール（層2）の責務
- ❌ コア移動の実行 → 層3の責務
- ❌ リフレッシュ時のコア回収処理 → 層3の責務
- ❌ コア獲得時の判定 → 層3の責務
- ❌ 特殊なコア管理ルール → 将来のルール（CoreLimitRule等）の責務

**除外エリア（Excluded Zone）の定義:**

除外エリアは、ゲーム進行中に除外されたカードを置く領域です。通常のゲーム領域から分離された特殊な場所です。

**構造:**

```
PlayerState 内の除外エリア

excluded: Card[]    ← 除外されたカード
```

**責務:**

1. **除外エリアの存在を定義する**
   - PlayerState 内に excluded: Card[] として保持する
   - ゲーム内の正式なゾーンとして定義する

2. **除外されたカードを保持する**
   - 除外されたカードのみを保有
   - 複数のカードを順序付きで保持可能

3. **ゲーム領域外の領域であることを定義する**
   - 通常のゲーム領域（デッキ、手札、フィールド、トラッシュ）とは別
   - 特別な指示がない限り通常利用しない

**トラッシュとの違い:**

| 項目 | トラッシュ | 除外エリア |
|------|----------|---------|
| 役割 | 通常の廃棄場所 | 除外カードの隔離 |
| 対象 | カード + コア | カードのみ |
| ゲーム領域 | ゲーム領域内 | ゲーム領域外 |
| 参照頻度 | 継召など頻繁 | 特別な効果のみ |
| コア戻し | リフレッシュ時に reserves へ | 通常リセットしない |

**ゾーン間のカード移動ルール:**

```
【カード除外】
field → excluded（条件により）
trash.cards → excluded（条件により）
hand → excluded（条件により）
```

**除外エリアが担当しないもの:**
- ❌ カードを除外する条件判定 → 層3の責務
- ❌ 除外されたカードが戻れるかの判定 → ゲーム進行/効果エンジンの責務
- ❌ 継召などの特殊処理 → 効果エンジン（層3.2）の責務
- ❌ ゲーム終了判定 → 層3の責務
- ❌ 効果処理 → 効果エンジンの責務
- ❌ 除外エリアからのカード移動処理 → 層3の責務
- ❌ 除外エリアの可視化 → 層4（API/UI）の責務

---

#### 軽減シンボルのルール（コスト支払い全般）

**定義:**
軽減シンボルは「カードの召喚、配置、または使用するコストを支払う際、軽減シンボルの数まで、自分のフィールドに対応するシンボルがあればコストを軽減します。」

**重要な制約:**
「フィールドにある軽減シンボルを使わずにコストを過剰に払うことはできません。」

**計算方法:**

```
実際に支払うコスト = max(0, カードのコスト - 軽減額)

軽減額 = min(
  カードの軽減シンボル数上限,
  フィールドの対応するシンボル数
)
```

**例：ゲン＝ガタの場合**

```
カード情報：
- コスト：4
- 軽減シンボル数：3（最大3まで軽減可能）
- 軽減シンボル色：赤

【シナリオ1】フィールドに赤シンボル2個ある場合
- 軽減額 = min(3, 2) = 2
- 支払うコスト = 4 - 2 = 2コア

【シナリオ2】フィールドに赤シンボル5個ある場合
- 軽減額 = min(3, 5) = 3
- 支払うコスト = 4 - 3 = 1コア

【シナリオ3】フィールドに赤シンボル0個ある場合
- 軽減額 = min(3, 0) = 0
- 支払うコスト = 4 - 0 = 4コア
```

**制約の重要性:**

「フィールドに赤シンボル3個あるのに、軽減を使わずに4コアを支払う」ことはできない。  
必ず軽減が適用される。

**層2での実装:**

```typescript
function calculateActualCost(
  cardCost: number,
  cardReductionLimit: number,
  fieldSymbolCount: number
): number {
  const reductionAmount = Math.min(cardReductionLimit, fieldSymbolCount);
  return Math.max(0, cardCost - reductionAmount);
}
```

---

#### ライフダメージのルール（将来BattleRuleへ統合予定）

**定義:**
攻撃時のライフダメージは、攻撃するスピリットの通常シンボル数（`symbol.count`）によって決定される。

**計算方法:**

```
ライフダメージ = 攻撃スピリットの symbol.count
```

**重要な設計決定:**
- ダメージ値は Card や CardLevel に含まない（BPと独立した概念）
- BP（攻撃力）と ライフダメージ（symbol数）は別のメカニクス
- symbol.count はルール層で参照され、攻撃結果を決定する

**例：ゲン＝ガタの場合**

```
ゲン＝ガタの通常シンボル数: 2個
攻撃時のライフダメージ: 2
（BP 5000 とは独立）
```

**将来の統合予定:**

このルールは、将来 **BattleRule** を導入する際に統合される予定です。

```
現在（ゲン＝ガタ段階）:
  層2: ライフダメージルール（独立）
  
将来（BattleRule導入時）:
  層2: BattleRule
       ├─ アタック管理
       ├─ ブロック管理
       ├─ BP比較
       ├─ 消滅判定
       └─ ライフダメージ計算 ← ここに統合
```

BattleRule導入後も、LifeRuleとの責務分離原則は変わりません：

| 責務 | BattleRule | LifeRule |
|------|:---:|:---:|
| ライフダメージ計算 | ✅ | — |
| ダメージ値をLifeRuleへ通知 | ✅ | — |
| ライフゾーン上のコア数解釈 | — | ✅ |
| 通知されたダメージを処理 | — | ✅ |
| ライフ0状態を通知 | — | ✅ |

---

#### LifeRule（ライフルール）

**定義:**
ライフゾーン上のコアをライフとして扱う独立した基本ルール

**責務:**

1. **ライフゾーン上のコア数をライフ値として解釈する**
   - ライフゾーン上に置かれたコアの数 = ライフ値

2. **ライフゾーンのコア減少をライフ減少として定義する**
   - ライフゾーン上のコアが取り除かれる = ライフが減少する
   - ダメージ計算はこのルールの責務ではない（層3の責務）

3. **ライフゾーンのコア増加をライフ増加として定義する**
   - ライフゾーン上へコアが置かれる = ライフが増加する
   - コアの移動はこのルールの責務ではない（CoreRuleの責務）

4. **ライフゾーン上のコア数が0になったことを通知する**
   - ライフゾーンのコア数が0 → ライフが0の状態を通知
   - ゲーム終了判定は層3が実行

5. **ライフに関する共通ルールを管理する**
   - ライフの初期値定義
   - ライフ変更時の共通処理・制約チェック

**重要な設計方針:**
- LifeRule はゲーム終了を行わない（層3の責務）
- LifeRule はコアを移動しない（CoreRuleの責務）
- LifeRule はゾーンを管理しない（ZoneStateRuleの責務）
- LifeRule はライフゾーンの状態から意味を読み取る解釈層

**ZoneStateRule との責務分離:**

| 責務 | ZoneStateRule | LifeRule |
|------|:---:|:---:|
| ゾーン存在定義 | ✅ ライフゾーンという領域が存在することを定義 | — |
| ゾーン管理 | ✅ 各ゾーン（手札、デッキ、場など）の存在・種類を管理 | — |
| ゾーン状態管理 | ✅ ゾーンへのカード・コアの出入りを管理 | — |
| ライフゾーンのコア数解釈 | — | ✅ ライフゾーン上のコア数をライフ値として解釈 |
| ライフ状態の提供 | — | ✅ ライフゾーンの状態からライフイベントを提供 |

**CoreRule（将来）との責務分離:**

| 責務 | CoreRule | LifeRule |
|------|:---:|:---:|
| コアの生成 | ✅ | — |
| コアの移動 | ✅ | — |
| コアの配置 | ✅ | — |
| 各ゾーン間のコア移動管理 | ✅ | — |
| ライフゾーン状態の参照 | — | ✅ |
| ライフゾーン上のコアをライフとして解釈 | — | ✅ |
| ライフイベントの提供 | — | ✅ |

**【ゲン＝ガタでの簡略実装方針】**

ゲン＝ガタでは実装を簡略化するため、PlayerState の `life: number` フィールドでライフゾーン上のコア数を表現して構いません。

ただし、この数値は：
- 「ライフゾーン上のコア数を簡略化した表現」であることを明記します
- 概念上は「ライフゾーン上のコア」と同等です

```
ゲン＝ガタ（簡略版）:
  PlayerState {
    life: number  ← ライフゾーン上のコア数を表現
  }

正式実装（Battle Spirits）:
  PlayerState {
    zones: {
      life: LifeZone {
        cores: Core[]  ← ライフゾーン上のコア
      }
    }
  }
  
LifeRule は両者で同じ概念を使用：
  「ライフゾーン上のコアをライフとして扱う」
```

**正式実装への移行:**

この設計により、正式版Battle Spiritsへ自然に移行できます：

1. **CoreRule の実装**
   - コアの生成・移動・配置を担当

2. **ZoneStateRule の拡張**
   - LifeZone を含む全ゾーンを管理

3. **LifeRule の移行**
   - ライフゾーン上のコア数からライフ値を読み取る層として機能
   - CoreRule/ZoneStateRule に依存する独立したルールとして機能
   - 責務分離の原則は変わらない

この段階的設計により、ゲン＝ガタの最小実装と正式版Battle Spiritsの概念を統一できます。

---

#### フェーズシステム

**定義:**
ゲームの進行をフェーズ単位で管理し、各フェーズでの許可アクションを制御するルール。

**フェーズの定義:**

- **start**: ゲーム開始処理
- **draw**: ドローフェーズ
- **main1**: メインフェーズ
- **attack**: アタックフェーズ
- **main2**: セカンドメインフェーズ
- **end**: エンドフェーズ

**各フェーズでの許可アクション:**

| フェーズ | 許可アクション | 説明 |
|---------|:---:|---|
| start | 自動遷移 | ゲーム開始処理 |
| draw | 自動ドロー | 1枚ドロー |
| main1 | 召喚、配置、魔法、pass | 任意のアクション |
| attack | 攻撃、pass | スピリット攻撃 |
| main2 | 召喚、配置、魔法、pass | メインフェーズ相当 |
| end | 自動リフレッシュ | ターン終了処理 |

**フェーズ遷移ルール:**

```
start → draw → main1 → attack → main2 → end → 
（次プレイヤーのstartへ）
```

**層2での実装:**

層2は「フェーズの定義」のみを提供します。

- ✅ フェーズの種類を定義
- ✅ 各フェーズでの許可アクションを定義
- ✅ フェーズ遷移ルール（順序）を定義

層2が定義しないこと：

- ❌ いつフェーズを遷移するか（層3の責務）
- ❌ フェーズ開始時の自動処理（層3の責務）
- ❌ カード効果によるフェーズ変更（層3.2の責務）

---

#### 誘発条件（トリガー）の管理

**定義:**
カード効果がゲーム中のイベントに反応する仕組みを管理するルール。

**層1での定義:**

層1は、カード効果が `trigger` 情報を持てるデータ構造のみを定義します。

```typescript
// 層1での定義
type Effect = {
  trigger: string;      // 誘発条件を示す情報を持てる
  action: string;
};
```

**層3.2での定義:**

トリガーの具体的な種類と実装は層3.2（効果エンジン）で定義・管理されます。

```typescript
// 層3.2での定義例
enum Trigger {
  SUMMON = 'summon',         // 召喚時
  ATTACK = 'attack',         // アタック時
  DESTROY = 'destroy',       // 破壊時
  LIFE_DECREASE = 'lifeDecrease',  // ライフ減少時
  TURN_START = 'turnStart',  // ターン開始時
  // ... 将来追加可能
}
```

**責務分離:**

| 層 | 誘発条件関連の責務 |
|----|---|
| **層1** | データ構造のみ（trigger情報を持てること） |
| **層3.2** | トリガーの種類定義、イベント監視、効果発動処理 |
| **層3.5** | 各カードが利用するトリガーを設定 |
| **層3** | イベント発生時にEffectEngineを呼び出し |

---

#### 基本アクション

**定義:**
ゲームの基本的なアクション（召喚、攻撃、コア管理）のルール。

**責務:**

1. **召喚（ゲン＝ガタでは固定）**
   - コスト4消費
   - 軽減シンボル3個まで軽減可能
   - スピリットを場に配置

2. **攻撃（ゲン＝ガタでは固定）**
   - ダメージ1与える
   - symbol.countではなく固定値

3. **コア管理（ゲン＝ガタでは固定）**
   - 毎ターン +1
   - ダメージ受時に +（ダメージ量）

---

### 層3: ゲーム進行（状態管理）

「ゲームがどう進むのか」を制御。状態遷移、アクション実行、進行管理。

**責務:**

1. **フェーズ進行管理**
   - 現在のフェーズを管理
   - フェーズ遷移を実行
   - 各フェーズの自動処理（ドロー、コア +1 など）

2. **アクション実行**
   - プレイヤーのアクションを受け取る
   - 層2のルールに従って実行
   - 状態を更新

3. **フラッシュウィンドウ制御**
   - アタックフェーズ中、フラッシュタイミング発動
   - プレイヤーに「フラッシュするか？」を選択させる

4. **勝利条件判定（新規追加）**
   - 各Ruleから通知を受け取る
   - 勝利条件を総合判定する
   - ゲーム終了判定を実行する

5. **ゲーム終了処理（新規追加）**
   - GameState を更新する
   - ゲーム終了判定を実行

**ゲーム終了処理の詳細:**

```
1. 各Ruleから勝敗に関する通知を受け取る
   ├─ LifeRule: ライフが0の通知
   ├─ ZoneStateRule: デッキ切れの状態
   └─ その他の勝敗条件（将来）

2. 勝利条件を総合判定する
   if (state.players[0].life <= 0) {
     gameState.gameOver = true;
     gameState.winner = 1;
   }
   if (state.players[1].life <= 0) {
     gameState.gameOver = true;
     gameState.winner = 0;
   }

3. GameState を更新する
   ├─ gameOver = true
   └─ winner を決定

4. ゲーム終了処理を実行
   ├─ 終了画面表示（UI）
   └─ リプレイ機能（将来）
```

**デッキゾーンとの関係:**

```
ゲーム開始時：
  ├─ PlayerState 生成
  ├─ デッキゾーン生成 (deck = [...])
  └─ 初期手札取得処理

ドローフェーズ：
  ├─ ZoneStateRule に問い合わせ
  │  └─ "deck -> hand への移動は許可されるか？"
  ├─ 許可 → カード取得
  └─ デッキが空になったら勝敗判定へ
```

**ゲン＝ガタ段階での実装:**
- ✅ ターン順序管理
- ✅ ドロー・コア +1 の自動実行
- ✅ フェーズ遷移
- ✅ フラッシュタイミング発動処理（効果なし）
- ✅ ゲーム終了判定（ライフ0判定）
- ✅ デッキゾーン管理（ドロー処理）

---

### 層3.2: 効果エンジン（カード効果実行）

**現段階での実装:**

ゲン＝ガタでは「効果なし」のため、効果エンジンの実装は不要です。

**将来の実装予定:**

カード効果を統一的に実行するエンジン。

```typescript
// 将来
type EffectAction = {
  trigger: 'summon' | 'attack' | 'destroy' | 'flash';
  action: string;
  execute: (state: GameState, context: ExecutionContext) => GameState;
};
```

**責務:**
- trigger（いつ発火するか）で分類
- action（何をするか）を実行
- minLevel などの条件判定

**将来の拡張:**
- 継召効果
- フラッシュ効果
- スキル効果
- etc.

---

### 層3.5: カード定義

カードデータベース。ゲーム中に参照される。

```typescript
const CARDS = [
  {
    id: 'spirit_gun_gata',
    name: 'ゲン＝ガタ',
    cardType: 'spirit',
    cost: 4,
    reduction: { symbolCount: 3, symbolColors: ['red'] },
    lineage: ['風牙'],
    text: '効果なし',
    levels: [
      { level: 1, bp: 5000 },
      { level: 2, bp: 7000 }
    ],
    attribute: '赤',
    effects: [],  // ゲン＝ガタでは効果なし
    cardNumber: '26RSD01-002',
    rarity: 'C',
    blockIcon: 'entry-deck',
    artworkIcon: 'rd01'
  },
  // ... more cards
];
```

**ゲン＝ガタ段階:**
- ✅ ゲン＝ガタのみ（効果なし）

**将来の拡張:**
- 継召持ちカード
- フラッシュ効果カード
- etc.

---

### 層4: API・UI連携

Express API、React コンポーネント。

**責務:**
- GameState → JSON 変換
- UI コンポーネントに送信
- プレイヤーのアクション受け取り

**ゲン＝ガタ段階:**
- ✅ GameBoard, PlayerPanel の表示
- ✅ アクション送信

---

## 🔄 データフロー（ゲン＝ガタ）

```
層0: MatchState
  └─ ゲーム1開始
      ↓
層1: GameState（初期化）
  ├─ players[0].life = 5
  ├─ players[0].hand = 4枚
  ├─ players[0].deck = 20枚
  ├─ players[0].reserves = 0
  └─ currentPhase = 'start'
      ↓
層3: ゲーム進行
  ├─ フェーズ遷移（start → draw → main1 → attack → main2 → end）
  ├─ 各フェーズで自動処理
  ├─ プレイヤーのアクション実行
  ├─ 層2 のルールに従って状態更新
  └─ 勝敗判定
      ↓
層2: 基本ルール
  ├─ 「召喚できるか」判定（軽減シンボルルール）
  ├─ 「攻撃ダメージ計算」実行（ライフダメージルール）
  ├─ 「ライフ0になった」通知（LifeRule）
  └─ 「デッキが移動できるか」確認（ZoneStateRule）
      ↓
層4: API・UI
  └─ GameState を JSON 化して送信
```

---

## 🎯 「ここまでは確定」宣言

**以下の設計は実装前に確定しました：**

- ✅ 層0: MatchConfig, MatchState
- ✅ 層1: PlayerState, Card, CardLevel, Effect, GameState
- ✅ 層2: フェーズシステム、基本ルール、LifeRule、誘発条件、ZoneStateRule
- ✅ 層3: ゲーム進行管理、勝敗判定、ゲーム終了処理
- ✅ 層3.2～4: 役割定義（詳細は後）

**ルール確認中に発見があれば、この設計を修正します。**

---

## 📋 ルール確認チェックリスト

ルールブック確認で以下を検証：

```
[✅] 初期生命：5 で正しいか
[✅] 初期手札：4 枚で正しいか
[✅] ゲン＝ガタ BP：5000 で正しいか
[✅] ゲン＝ガタ ダメージ：1 で正しいか
[✅] ゲン＝ガタ コスト：4 で正しいか
[✅] 毎ターン +1 コアで正しいか
[✅] ダメージ受時の +コアで正しいか
[✅] フェーズ順序：start → draw → main1 → attack → main2 → end で正しいか
[✅] フラッシュタイミング：attack のみで正しいか
[✅] その他確認事項
```

---

## 🚀 次のステップ

1. **ルールブック確認を続ける**（項目ごと）
2. **発見があれば、この設計を修正**
3. **設計確定後、層2～4 の詳細設計**
4. **実装開始**

---

## 参考
